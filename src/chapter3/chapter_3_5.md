# Sets

Sets are unordered collections of unique objects.
Sets are mutable. New elements can be added and deleted to the set

```python
set_ = {'a', 123, 'a', "String"}
print(set_)
set_.add('xyz')
print(set_)

```
Output
```
{'a', 123, "String"}
{'a', 123, 'xyz', "String"}

```
